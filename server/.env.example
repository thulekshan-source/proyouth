# Copy this file to .env and fill in your real values:
#   cp .env.example .env
# (.env is git-ignored so your password is never committed)

PORT=5000

# From Atlas: Database Deployments -> Connect -> Drivers -> copy connection string,
# then replace <db_password> with your database user's password.
# URL-encode special characters in the password: @ -> %40, # -> %23, $ -> %24, % -> %25
MONGODB_URI="mongodb+srv://kirupairasathulekshan9_db_user:<db_password>@cluster0.iptjnmz.mongodb.net/proyouth"

# Secret used to sign login tokens — change this to any long random string
JWT_SECRET="super-secret-key-for-development"
